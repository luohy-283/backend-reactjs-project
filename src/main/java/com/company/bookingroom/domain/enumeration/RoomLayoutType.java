package com.company.bookingroom.domain.enumeration;

/**
 * Physical room layout / size class (independent of seat capacity).
 */
public enum RoomLayoutType {
    COMPACT,
    STANDARD,
    SPACIOUS,
    BOARDROOM,
    AUDITORIUM,
}
